<th:block th:insert="~{/shared/_header.html}"></th:block>

<div class="login-form">
    <!-- Login Form -->
    <form class="auth-form login-form" method="post" th:object="${loginRequestDto}">
    <div>
        <img src="https://i.pinimg.com/736x/11/e2/c1/11e2c18430aec77d4ae9f1b1a7e40ec8.jpg"
             style="height: 320px; width: 300px; border-radius: 20px 0px 0 20px ">
    </div>
    <div class="form-box">
        <h2>Login</h2>

            <div class="textbox">
                <input class="form-control signin-email" maxlength="50" minlength="3"
                       placeholder="Email address" required="required" th:field="*{email}" type="email">
                <span class="text-danger" th:errors="*{email}"
                      th:if="${#fields.hasErrors('email')}"></span>
            </div>
            <div class="textbox">
                <input class="form-control signin-password" maxlength="50"
                       minlength="6" pattern="^(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d]{6,50}$" placeholder="Password"
                       required="required" th:field="*{password}" type="password">
                <span class="text-danger" th:errors="*{password}"
                      th:if="${#fields.hasErrors('password')}"></span>
            </div>
            <input class="btn" type="submit" value="Login"></form>
        <p>Don't have an account? <a th:href="@{/sign-up}">Sign up</a></p>
    </div>
</div>

<th:block th:insert="~{/shared/_footer.html}"></th:block>